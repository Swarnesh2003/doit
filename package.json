{
  "name": "doit",
  "displayName": "doIt",
  "description": "A visual studio code extension used to automate the process of project creation and building based on the given instructions by integrating it with chatgpt",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.95.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./extension.js",
  "contributes": {
    "commands": [{
      "command": "doit.helloWorld",
      "title": "Hello World"
    },
    {
      "command": "extension.openSearchTab",
      "title": "Open Search Tab"
    }
  ]
  },
  "scripts": {
    "lint": "eslint .",
    "pretest": "npm run lint",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.95.0",
    "@types/mocha": "^10.0.9",
    "@types/node": "20.x",
    "eslint": "^9.13.0",
    "@vscode/test-cli": "^0.0.10",
    "@vscode/test-electron": "^2.4.1"
  }
}
